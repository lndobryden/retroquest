language: java
jdk:
  - oraclejdk8

sudo: required

services:
  - mysql

addons:
  chrome: stable

cache:
  directories:
    - ./ui/node_modules
    - $HOME/.m2
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
script:
  - ./prepare.sh
